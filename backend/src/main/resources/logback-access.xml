<configuration>
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%date{yyyy-MM-dd HH:mm:ss.SSS}  %green(INFO) %magenta(${PID:- }) --- [nio-8080-exec-1] %cyan(o.s.web.servlet.DispatcherServlet)        : REQUEST
%requestURL
    Accept: %header{Accept}
    Content-Type: %header{Content-Type}
    Authorization: %header{Authorization}
    %magenta(%requestContent)

HTTP/1.1 %statusCode
    %cyan(%responseContent)
            </pattern>
        </encoder>
    </appender>
    <appender-ref ref="STDOUT"/>
</configuration>
